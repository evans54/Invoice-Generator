{
  "name": "invoice-generator-backend",
  "version": "1.0.0",
  "description": "Simple backend for Invoice Generator (PDF export + currency labeling)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build step required for this app'",
    "prod": "NODE_ENV=production node server.js",
    "install-deps": "npm install --production=false"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "express-rate-limit": "^7.1.5",
    "helmet": "^7.1.0",
    "pdfkit": "^0.13.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
